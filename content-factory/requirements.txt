requests>=2.31
python-dotenv>=1.0
Pillow>=10.0
pytest>=8.0

# Только для tools/sheets_sync.py (S5) — импортируются лениво внутри
# GoogleSheetsClient, поэтому не нужны для остального конвейера и тестов.
google-auth>=2.30
google-api-python-client>=2.130
